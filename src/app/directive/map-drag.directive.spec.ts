import { MapDragDirective } from './map-drag.directive';

describe('MapDragDirective', () => {
  it('should create an instance', () => {
    const directive = new MapDragDirective();
    expect(directive).toBeTruthy();
  });
});
